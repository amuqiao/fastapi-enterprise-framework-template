# 11. 命令速查表

本章节提供 Docker 常用命令的速查表，方便快速查询和使用。

## 11.1 常用命令

包含 Docker 最常用的基本命令，涵盖镜像管理、容器管理、网络管理和数据卷管理等方面。

详细内容请参考：[11.1 常用命令](./11.1.常用命令.md)

## 11.2 Docker Compose 常用命令

包含 Docker Compose 的常用命令，涵盖服务管理、构建部署、日志监控和资源清理等方面。

详细内容请参考：[11.2 Docker Compose常用命令](./11.2.Docker Compose常用命令.md)

## 11.3 资源清理命令

包含 Docker 资源清理的各种命令，帮助您释放存储空间，保持 Docker 环境的整洁。

详细内容请参考：[11.3 资源清理命令](./11.3.资源清理命令.md)

## 11.4 命令使用技巧

### 11.4.1 命令别名

为常用命令设置别名，可以提高工作效率：

```bash
# 在 ~/.bashrc 或 ~/.zshrc 中添加以下内容
alias d='docker'
alias dps='docker ps'
alias dpa='docker ps -a'
alias di='docker images'
alias drun='docker run -d --name '
alias dstop='docker stop '
alias drm='docker rm '
alias drmi='docker rmi '
alias dlogs='docker logs -f '
alias dexec='docker exec -it '
alias dinspect='docker inspect '
alias dcompose='docker compose '
```

### 11.4.2 命令补全

启用 Docker 命令补全，可以通过按 Tab 键自动补全命令、容器名称、镜像名称等：

```bash
# 安装命令补全（Ubuntu/Debian）
sudo apt-get install bash-completion
sudo curl -L https://raw.githubusercontent.com/docker/cli/master/contrib/completion/bash/docker -o /etc/bash_completion.d/docker
source /etc/bash_completion

# 安装命令补全（CentOS/RHEL）
sudo yum install bash-completion
sudo curl -L https://raw.githubusercontent.com/docker/cli/master/contrib/completion/bash/docker -o /etc/bash_completion.d/docker
source /etc/bash_completion
```

### 11.4.3 命令组合使用

通过管道和命令替换，可以组合多个命令，实现更复杂的功能：

```bash
# 查看容器日志并过滤关键字
docker logs -f mycontainer | grep ERROR

# 删除所有已停止的容器
docker rm $(docker ps -a -q)

# 删除所有悬空镜像
docker rmi $(docker images -f dangling=true -q)

# 进入正在运行的容器
docker exec -it $(docker ps -q) bash
```

### 11.4.4 查看命令帮助

使用 `--help` 选项查看命令的详细帮助信息：

```bash
docker --help
docker run --help
docker-compose --help
```

### 11.4.5 在线文档

访问 Docker 官方文档，获取更详细的命令说明和示例：

- [Docker 官方文档](https://docs.docker.com/)
- [Docker Compose 官方文档](https://docs.docker.com/compose/)

## 11.5 速查表使用建议

1. **打印或保存**：将速查表打印出来或保存到本地，方便随时查阅
2. **分类记忆**：按照功能分类记忆命令，如镜像管理、容器管理、网络管理等
3. **实践使用**：通过实际操作加深对命令的理解和记忆
4. **定期复习**：定期复习命令，避免遗忘
5. **更新内容**：随着 Docker 版本的更新，命令可能会有变化，定期更新速查表内容

通过熟练掌握这些命令，可以大大提高您使用 Docker 的效率，更好地管理和维护 Docker 环境。